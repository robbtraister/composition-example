{
  "name": "composition-bundle",
  "version": "0.0.0",
  "main": "server.js",
  "scripts": {
    "db": "docker run --rm --name composition -p 27017:27017 -v \"$(npm prefix)\"/data/db:/data/db:rw -v \"$(npm prefix)\"/data/dumps:/data/dumps:rw -v \"$(npm prefix)\"/data/restore:/data/restore:rw robbtraister/mongo-vandelay",
    "dev": "compose dev",
    "dump": "docker exec $(docker ps --filter name=^composition$ --format '{{.ID}}') ./dump.sh",
    "prod": "compose prod",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/react": "^16.9.17",
    "react": "^16.12.0",
    "react-router-dom": "^5.1.2"
  },
  "description": "",
  "dependencies": {
    "@composition/composer": "0.0.1-beta.9"
  }
}
